{{ for using in Usings ~}}
using {{ using }};
{{ end ~}}

namespace {{Namespace}}
{
    public static class AlpakaAssemblyIdentifier
    {
        public static IEnumerable<AlpakaEventTypeComposite> GetAlpakaEventTypeComposites() 
        {
            {{ for alpakaEvent in AlpakaEvents ~}}
             yield return new AlpakaEventTypeComposite(typeof({{alpakaEvent}}), typeof({{alpakaEvent}}ReceiverBase), typeof({{alpakaEvent}}Emitter));
            {{ end ~}}
        }
    }
}