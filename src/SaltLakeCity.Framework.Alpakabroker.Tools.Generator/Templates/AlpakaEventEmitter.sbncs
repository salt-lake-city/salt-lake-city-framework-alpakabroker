{{ for using in Usings ~}}
using {{ using }};
{{ end ~}}

namespace {{Namespace}}
{
    public static class {{EventName}}Emitter
    {
       public static void Emit{{EventName}}({{EventName}} e) 
       {
            
       }
    }
}